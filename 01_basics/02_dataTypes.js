"use strict"; // treat all JS code as newer version

// alert("Hello World!") // We are using nodejs, not browser, so this will not work

console.log(3 
    + 
     3) // code readability should be high

console.log("Hello World"); 

let name = "John Doe";
let age = 30;
let isLoggedIn = false;
let state = null; 

// number => 2 to power of 53
// bigInt
// string => ""
// boolean => true/false
// null => standalone value, empty value representation
// undefined => variable is declared but not assigned
// symbol => unique and immutable value, often used as object property keys


// object => collection of key-value pairs

console.log(typeof undefined); // undefined
console.log(typeof null); // object (this is a known bug in JS, null is not an object)